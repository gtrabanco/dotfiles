
if [[ "$TERM_PROGRAM" == "vscode" ]]; then
  shell_name="$(echo "$SHELL" | awk -F'/' '{print tolower($NF)}')"
  . "$(code --locate-shell-integration-path "$shell_name")"
  HISTFILE="${ZDOTDIR}/.vscode_history"
fi
